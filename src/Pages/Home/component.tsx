import React, { memo } from 'react';

const Home = () => {
    return (
        <>
            <div className="Home">
                <h1>Welcome Page</h1>
            </div>
        </>
    )
};

export default memo(Home);
