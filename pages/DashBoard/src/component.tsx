import React, { memo } from 'react';
import { BarChart } from "@pages/chart-js"

const DashBoard = () => {

    return (
        <>
            <BarChart />
        </>
    )
};

export default memo(DashBoard);
